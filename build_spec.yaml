version: 0.1
component: build

steps:
  - name: ValidarEEmpacotarScripts
    type: Command
    command: |
      echo "Empacotando scripts de banco de dados para a GMUD..."
      cat GMUD-1.txt
      mv GMUD-1.txt GMUD-1.sql

outputArtifacts:
  - name: GMUD-TESTE # Nome do artefato cadastrado no OCI DevOps
    type: BINARY
    location: ${OCI_PRIMARY_SOURCE_DIR}/GMUD-1.sql
